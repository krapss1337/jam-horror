using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioSettings : MonoBehaviour
{
    //We will make this after placing the audio files.

    //Probably we will save the audio preferances (settings) in a scriptable object. Then apply it within each scene.
}
